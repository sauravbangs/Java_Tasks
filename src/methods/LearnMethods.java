package methods;

public class LearnMethods {
    public static void main(String[] args) {
        // static keyword: class level property. Can be called without using the object.
        /* static keyword: if you want to call a method within a static method(like main), that method must also be
        a static method.*/
    }
}
